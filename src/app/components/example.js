export function ExampleGreeting({ name = "Diego" }) {
  return <h1>Hola {name}</h1>;
}

export function calculateArea(width, height) {
  return width * height;
}

export const fruits = ["apple", "banana", "orange"];
